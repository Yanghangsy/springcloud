server:
  port: 8080

spring:
  application:
    name: cloud-consumerConsul-order80
  cloud:
    consul:
      port: 8500
      host: localhost
      discovery:
        service-name: ${spring.application.name}